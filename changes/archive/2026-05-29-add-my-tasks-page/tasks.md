## 1. Routing Updates

- [x] 1.1 Locate the frontend routing configuration (e.g., `App.jsx` or similar router file).
- [x] 1.2 Add the route for `/mytasks` pointing to a new `MyTasksPage` component.

## 2. Page Component Implementation

- [x] 2.1 Create the `src/Pages/MyTasksPage.jsx` file.
- [x] 2.2 Implement the page structure mirroring the existing layout (using `Navbar` and a container).
- [x] 2.3 Implement the API fetch logic to retrieve tasks assigned to the current user (e.g., using `?user=<user_id>`).
- [x] 2.4 Render the fetched tasks in a list or table matching the existing application style.
