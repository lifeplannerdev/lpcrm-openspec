## 1. Setup & Dependencies

- [x] 1.1 Install form dependencies (`@react-native-picker/picker`, `@react-native-community/datetimepicker`)

## 2. API Integration

- [x] 2.1 Update `useTasks.ts` to include `useCreateTask` and `useUpdateTask` mutation hooks
- [x] 2.2 Create a hook `useStaffMembers.ts` to fetch staff for the assignment dropdown

## 3. UI Implementation

- [x] 3.1 Create `TaskDetailsScreen.tsx` with Glassmorphic styling and a Status update dropdown
- [x] 3.2 Create `TaskFormScreen.tsx` (handles both creation and editing)
- [x] 3.3 Update `MainScreens.tsx` to implement a Stack Navigator for the Tasks flow (List -> Details -> Form)
- [x] 3.4 Add a Glassmorphic Floating Action Button (FAB) to the main `TasksScreen` for authorized users
