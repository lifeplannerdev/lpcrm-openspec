## 1. Navigation Configuration

- [x] 1.1 Update `mobileMasterNavigation` in `MainTabNavigator.tsx` to include all 13 CRM permissions matching `roles.js`

## 2. Placeholder Screen Creation

- [x] 2.1 Create `HrScreens.tsx` with `PenaltiesScreen` and `AttendanceDocsScreen` components using Purple and White design
- [x] 2.2 Create `ReportScreens.tsx` with `StaffReportsScreen` and `VoxbayScreen` components
- [x] 2.3 Create `CandidatesScreen.tsx` with `CandidatesScreen` component
- [x] 2.4 Create `AttendanceScreen.tsx` with `MarkAttendanceScreen` component

## 3. Wiring and Validation

- [x] 3.1 Register all new screens in `MainTabNavigator` component map
- [x] 3.2 Update the Demo Bypass login to grant all 13 permissions for testing
- [x] 3.3 Validate the "More Menu" successfully displays the 9 overflow screens
