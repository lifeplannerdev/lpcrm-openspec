## 1. Backend Verification

- [x] 1.1 Verify or implement an endpoint to fetch active students (`/trainers/students/` or similar) that returns id and name.

## 2. Frontend Implementation

- [x] 2.1 Update `FeesManagementPage.jsx` to fetch the list of students on mount (similar to templates and accounts).
- [x] 2.2 Add `students` to component state.
- [x] 2.3 Replace the text `<input>` for Student ID in `FeesManagementPage.jsx` with a searchable dropdown or `<select>` mapping over the fetched `students` array.
- [x] 2.4 Verify that selecting a student properly sets `createForm.student` and allows successful fee account creation.
