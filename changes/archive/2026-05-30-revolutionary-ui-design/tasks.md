## 1. Setup & Dependencies

- [x] 1.1 Install `expo-linear-gradient`, `expo-blur`, and `react-native-reanimated`

## 2. Core UI Infrastructure

- [x] 2.1 Refactor `ScreenWrapper.tsx` to use `LinearGradient` as the base background
- [x] 2.2 Create a reusable `AnimatedGlassCard` component utilizing `BlurView` and `react-native-reanimated` press animations

## 3. Screen Redesigns

- [x] 3.1 Refactor `MenuScreen.tsx` grid to use `AnimatedGlassCard` for the premium look
- [x] 3.2 Refactor `DashboardScreen.tsx` to utilize the new mesh gradient background and glass panels
- [x] 3.3 Ensure the Purple and White branding remains prominent but modernized
